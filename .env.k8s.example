# Kubernetes Deployment Configuration
# Copy this file and update with your actual values before deployment

# Container Registry Settings
REGISTRY=your-registry.example.com
IMAGE_NAME=industrial-automation-ui
IMAGE_TAG=v1.0.0

# Kubernetes Settings
NAMESPACE=automation
REPLICAS=2

# Application Configuration
# These will be set in the ConfigMap (k8s/deployment.yaml)
API_URL=http://automation-api:8080
MQTT_WS_URL=ws://mqtt-broker:9001

# Ingress Configuration
DOMAIN=automation.example.com
TLS_SECRET_NAME=automation-ui-tls
INGRESS_CLASS=nginx

# Optional: cert-manager configuration
CERT_MANAGER_ISSUER=letsencrypt-prod

# Resource Limits
MEMORY_REQUEST=128Mi
MEMORY_LIMIT=256Mi
CPU_REQUEST=100m
CPU_LIMIT=500m
